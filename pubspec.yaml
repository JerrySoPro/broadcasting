name: broadcasting

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.0
  audioplayers: ^5.2.1
  photo_view: ^0.14.0
  cached_network_image: ^3.3.0
  battery_plus: ^5.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
    - assets/video/
